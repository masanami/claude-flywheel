# 課題台帳の読み込み方の見直し（#122 の判断材料）

> **本ドキュメントは判断材料であり、採否は含まない**（[#122](https://github.com/masanami/claude-flywheel/issues/122)）。
> 4 案それぞれについて、削減量の実測・手順1／手順2 が必要とする判断材料の充足・
> フォーマット契約と board パーサへの波及・ingest の `fp` への影響を並べ、
> **推奨**を付ける。決定は人間が行う。
>
> 前提となる文脈コストの実測は [run-cycle-context-budget.md](./run-cycle-context-budget.md)。
> 本ドキュメントはその §1.4（台帳の内訳）を**フィールド粒度まで**掘り下げたもの。

## 0. 要旨

1. **台帳の 65.9% は「説明の原文引用」1 フィールドである**（30,883 tok 中 20,339 tok）。
   エントリ本文だけを分母に取れば 68.5%。アーカイブでも同じ比率（65.2%）で、
   これは一時的な偏りではなく**構造的な内訳**である（§1.3）。
2. **案 1（ステータスによる絞り込み）は、そのままでは削減量ゼロになる**。
   7 ステータスすべてが、いずれかの手順の対象として生きているためである（§2.3）。
   削減は「どのエントリを読むか」ではなく「**各エントリのどこまで読むか**」で決まる。
3. **索引は 1 エントリ 70 tok で作れる**（全 11 エントリで 771 tok ＝ 全文 30,883 tok の
   **2.5%**）。実際に投影スクリプトを書いて実測した（§3.2）。1 エントリあたり
   2,698 tok → 70 tok で、**38 分の 1** になる。
4. **案 2 は「索引ファイルを持つ」形で実装してはならない**。それは
   「索引と本体を同期させる」構図になり、`sync-free` 不変条件に反する。
   **索引は台帳から毎回投影する**（ファイルを作らない・stdout のみ）形にすれば、
   正本は台帳 1 つのままで、フォーマット契約・board パーサ・`fp` への波及が
   **すべてゼロ**になる（§4）。
5. **案 3（原文引用を台帳から外す）は、既に一度潰した欠陥を再生産する**。
   ingest-challenges の `fp` 規定は「引用行を必ず含める」ことを明文で要求しており、
   外すと**外部 Issue の本文だけが編集されたとき更新検知が永久に効かなくなる**
   （skills/ingest-challenges/SKILL.md の当該規定が、その失敗をそのまま記述している）。
   さらに board のカード詳細が `-` 相当の表示に退行し（実コードで確認）、
   アーカイブの恒久記録から 65.2% が失われる（§3.3）。
   **案 2 とほぼ同じ削減量を、これらの破壊なしに得られる**。
6. **案 4（母数の削減）は継続すべきだが、基準線としては不十分**。台帳は
   2026-08-25 の 49,793 B から翌 08-26 に 152,987 B へ**1 日で 3.1 倍**に振れた実測がある
   （§1.4）。一度きりの剪定は、外部 ingest の流量に対して持続的な制御にならない。

## 1. 実測

### 1.1 計測方法（再現手順）

[run-cycle-context-budget.md §1.1](./run-cycle-context-budget.md) と**同一**の方法を使う——
`tiktoken` の `o200k_base` による代理指標であり、**Claude のトークナイザではない**。
バイト数を併記してあるので、別のトークナイザで数え直せる。

- 測定日: **2026-08-27 JST（夕）**。#121 の測定は同日**朝**であり、その間に台帳が
  1 エントリ増えている（§1.2 の差分はこれによる）。
- 測定対象: 稼働中のエージェントワークスペース（`<workspace-root>`）の実ファイル。
- 台帳の版: `challenge-ledger.md` 100,652 B / 30,883 tok / **エントリ 11 件**。

```bash
python3 -m pip install --user tiktoken
python3 - <<'PY'
import tiktoken; enc = tiktoken.get_encoding("o200k_base")
s = open("<workspace-root>/challenge-ledger.md", encoding="utf-8").read()
print(len(s.encode()), len(enc.encode(s)))   # => 100652 30883
PY
```

エントリ分割・フィールド分割は次の規則で行った（**validate-artifact.rb の
`annotate_exclusions` / エントリ境界 `^### \[` と同じ意味論**）:

- ``` ``` ``` フェンス内の行は除外する（台帳先頭の「記入例」は `### [C-001]` を含むが
  フェンス内のため**エントリとして数えない**）。
- エントリ境界は `^### \[` にマッチする行（[ledger-range-delete-bug] の教訓どおり
  行番号のオフセットでは切らない）。
- フィールドは `^- <ラベル>:` を起点に、次のフィールド行または `**人間記入欄**` /
  `**分類欄** ` までを 1 ブロックとする。ブロック引用行（`^>`）は「説明」ブロックに含まれる。

### 1.2 現在の固定文脈（毎周・無条件。2026-08-27 夕の再測定）

| 区分 | ファイル | バイト | **tok** | #121（同日朝） |
| --- | --- | ---: | ---: | ---: |
| 自動ロード | `CLAUDE.md` | 10,175 | 2,953 | 2,953 |
| 自動ロード | `memory/harness/INDEX.md` | 14,192 | 4,121 | 3,860 |
| スキル本文 | `skills/run-cycle/SKILL.md` | 96,080 | 27,652 | 27,652 |
| 手順0 ロード | **`challenge-ledger.md`** | 100,652 | **30,883** | 27,913 |
| 手順0 | `challenge-sources.md` | 4,789 | 1,425 | 1,425 |
| | **小計（毎周・無条件）** | **225,888** | **67,034** | 63,803 |

**台帳が固定文脈に占める割合は 46.1%**（30,883 / 67,034）で、SKILL.md（41.3%）を
**上回った**。#121 の時点では両者ほぼ同額だったが、**半日でこの順位が入れ替わっている**。

### 1.3 台帳の内訳（フィールド粒度・全 11 エントリの合計）

エントリ本文の合計は 29,679 tok（ヘッダ＋記入例 1,204 tok が別途ある）。

| フィールド | tok | エントリ本文に占める割合 |
| --- | ---: | ---: |
| **説明** | **20,449** | **68.9%** |
| タスク案 | 2,415 | 8.1% |
| 備考 | 2,181 | 7.3% |
| 完了条件（任意） | 1,644 | 5.5% |
| 構造行（`**人間記入欄**` / `**分類欄**` / 見出し / 空行） | 724 | 2.4% |
| 取り込み元 | 460 | 1.5% |
| 承認（人間がチェック） | 444 | 1.5% |
| 起票者 / 起票日 | 220 | 0.7% |
| 関連リポジトリ | 211 | 0.7% |
| 関連Issue | 178 | 0.6% |
| 体感の緊急度（任意） | 154 | 0.5% |
| 関連サービス | 128 | 0.4% |
| ステータス | 119 | 0.4% |
| 担当ポジション | 99 | 0.3% |
| 優先度 | 88 | 0.3% |
| 関連PR | 77 | 0.3% |

**「説明」20,449 tok のうち 20,339 tok（99.5%）はブロック引用行**（`^>` で始まる 930 行）
である。残る 110 tok はフィールド行 `- 説明:（原文引用）` そのもの（11 エントリ × 10 tok）。

> 索引に必要なフィールド（ステータス・優先度・担当ポジション・起票日・関連リポジトリ・
> 承認チェック）の合計は **1,181 tok（4.0%）** に過ぎない。

エントリ別（ステータスは 2026-08-27 夕時点）:

| エントリ | tok | うち引用 | ステータス |
| --- | ---: | ---: | --- |
| C-038 | 2,406 | 1,629 | 分類済 |
| C-040 | 2,152 | 1,440 | 着手中 |
| C-044 | 3,589 | 2,155 | 着手中 |
| C-054 | 1,483 | 820 | 計画承認待ち |
| C-056 | 4,197 | 3,179 | 着手中 |
| C-060 | 3,500 | 3,071 | 分類済 |
| C-061 | 1,164 | 736 | 分類済 |
| C-065 | 2,065 | 1,463 | 計画承認待ち |
| C-071 | 2,681 | 1,721 | 計画承認待ち |
| C-072 | 4,205 | 2,886 | 計画承認待ち |
| C-074 | 2,237 | 1,239 | 着手中 |
| **合計** | **29,679** | **20,339** | 11 件 |

平均 **2,698 tok / エントリ**（#121 の 2,671 とほぼ同じ）。ステータス別の小計:

| ステータス | 件数 | tok |
| --- | ---: | ---: |
| 着手中 | 4 | 12,175 |
| 計画承認待ち | 4 | 10,434 |
| 分類済 | 3 | 7,070 |

`challenge-archive.md`（57 エントリ・89,229 tok）でも引用行は **58,186 tok ＝ 65.2%** で、
台帳と同じ比率だった。**「説明の原文引用が大半」は恒常的な構造**である。

### 1.4 台帳サイズの推移（案 4 の効果と、その持続性）

`git show <SHA>:challenge-ledger.md` で各日の最終コミットを実測した
（`heads` は `grep -c '^### \['` ＝ フェンス内の記入例 1 件を含む生の見出し数）:

| 日付 | バイト | heads |
| --- | ---: | ---: |
| 2026-08-19 | 71,891 | 16 |
| 2026-08-21 | 80,194 | 13 |
| 2026-08-23 | 60,042 | 8 |
| 2026-08-25 | 49,793 | 7 |
| **2026-08-26** | **152,987** | **20** |
| 2026-08-27 | 99,317 | 12 |

**08-25 → 08-26 で 3.1 倍（+103,194 B ≒ +31,600 tok）に振れている。**
08-26 → 08-27 の縮小が `assignee_policy: self-only` への切り替え（案 4）による剪定である。
つまり案 4 は**振れ幅の片側を戻しただけ**で、翌日にはもう 99,317 B に戻っている。

## 2. 手順1・手順2 が必要とする判断材料（フィールド粒度）

### 2.1 現在の規定

手順0 は台帳の読み方を次の一文で規定している（`skills/run-cycle/SKILL.md`）:

> ベースライン（ポジション要約・記憶 INDEX・recall 手順）は `CLAUDE.md` として**自動ロード済み**。
> 加えて `challenge-ledger.md` をロードし、**詳細記憶は手順2の recall で関連分だけ**引く。

「ロード」に範囲の限定はなく、**絞り込みを許す規定も存在しない**。一方で
手順1〜4 の対象は見出しレベルでステータスに絞られている
（手順1「対象: ステータス『未分類』」、手順2「対象: ステータス『分類済』」…）。
**対象は絞られているのに読み込みは絞られていない**、というのが現状である。

### 2.2 規定 → 必要フィールドの対応表

`skills/run-cycle/SKILL.md` の手順1・手順2 の全規定を、必要とする台帳フィールドの
粒度で列挙する。「索引」＝ID・タイトル・ステータス・優先度・担当ポジション・起票日・
関連リポジトリ・承認チェックの `[ ]`/`[x]` 状態（§3.2 の投影で得られるもの）。

#### 手順1（整理）── 対象: ステータス「未分類」

| # | 規定 | 対象エントリ | 必要な入力 | 索引で足りるか |
| --- | --- | --- | --- | --- |
| 1-a | 担当ポジションの分類（守備範囲に入るか） | 未分類 | **説明**・関連サービス／記憶 `map` | **✗ 全文** |
| 1-b | 優先度 `P0/P1/P2` の判定（適用方針モードの判定ルール） | 未分類 | **説明**（P0: 本番障害・データ破損・セキュリティ影響／P1: 完了条件が明確で影響範囲が中〜大）・**完了条件**・体感の緊急度 | **✗ 全文** |
| 1-c | ステータスの記入（`未分類 → 分類済`） | 未分類 | ステータス | ✓ |
| 1-d | 要相談の判定と備考への理由記入 | 未分類 | **説明**（判断の対象）／記憶 `map` | **✗ 全文** |
| 1-e | 範囲外の見送り | 未分類 | **説明** | **✗ 全文** |
| 1-f | 承認プロトコル（`[x] 計画を承認` / `[x] 完了を承認` の検出と前進） | **計画承認待ち・完了確認待ち** | ステータス＋**承認チェックボックス 2 行** | **✓** |
| 1-g | 承認の真正性（`[x]` が人間のコミットで入ったか） | 同上 | `git log`（台帳本文ではない） | ✓（台帳不要） |
| 1-h | 差し戻しの検出（チェック無し＋人間記入欄／備考の修正指示） | 計画承認待ち・完了確認待ち | **備考／人間記入欄** | **✗ 部分（備考）** |
| 1-i | アーカイブ・バックストップ（`完了` から 30 日 or 20 件超） | **完了** | ステータス＋`git log`（基準日）。移動時のみ**エントリ全文**（原文のまま追記するため） | **✓ 判定は索引・移動時のみ全文** |

#### 手順2（計画）── 対象: ステータス「分類済」

| # | 規定 | 対象エントリ | 必要な入力 | 索引で足りるか |
| --- | --- | --- | --- | --- |
| 2-a | **着手順の自己選択**（適用方針モードの選択基準） | **分類済（全件を比較）** | モード依存。§2.4 参照 | **モード依存** |
| 2-b | モード切り替え時に既存エントリの優先度欄を再判定しない | 分類済 | 優先度 | ✓ |
| 2-c | 関連記憶の recall | — | 台帳不要（agent-memory へ委譲） | ✓（台帳不要） |
| 2-d | assignee 再照合（ingest-challenges 再実行） | 取り込み元マーカーのある全件 | **取り込み元マーカー＋`fp`**（読むのは ingest 側。親は ID 一覧だけでよい） | **✓（親としては）** |
| 2-e | 目標 → タスク分解し「タスク案」を追記 | **着手する 1 件** | **説明・完了条件・体感の緊急度・備考** | **✗ 全文（当該 1 件のみ）** |
| 2-f | 「関連リポジトリ」に `<owner>/<repo>` を記入 | 着手する 1 件 | `repos.tsv`＋**説明**（対象の同定） | **✗ 全文（当該 1 件のみ）** |
| 2-g | 【承認ゲート FR-13】承認粒度は `タスク案` / `完了条件` / `関連リポジトリ` の 3 つ | 着手する 1 件 | タスク案・完了条件・関連リポジトリ | **✗ 部分（完了条件）** |
| 2-h | ステータス → 計画承認待ちにして保留・報告 | 着手する 1 件 | ステータス | ✓ |

**含意**: 全文が要るのは (i) **未分類エントリ**（1-a/1-b/1-d/1-e）と
(ii) **その周に実際に着手する 1〜数件**（2-e/2-f/2-g）だけである。それ以外の規定は
索引または `git log` で足りる。手順3（実行）は対象エントリのみを、手順4（検証）は
アーカイブ移動時に対象エントリの原文を必要とし、いずれも**他エントリを参照しない**
（`skills/run-cycle/SKILL.md` の手順3・手順4 に横断参照の規定は無い）。

### 2.3 「ステータスで絞る」が単独では効かない理由

7 つのステータスすべてが、いずれかの手順の**対象**として生きている:

| ステータス | 消費する規定 |
| --- | --- |
| 未分類 | 手順1（整理）の対象 |
| 分類済 | 手順2（計画）の対象 |
| 計画承認待ち | 手順1-f（`[x] 計画を承認` の検出 → 着手中へ前進） |
| 着手中 | 手順3（実行）の対象 |
| 検証中 | 手順4（検証）の対象 |
| 完了確認待ち | 手順4 FR-32（`[x] 完了を承認` の検出 → 完了＋即アーカイブ） |
| 完了 | 手順1-i（アーカイブ・バックストップ） |

**どのステータスも捨てられない**。したがって「当周に扱いうるステータスのエントリだけを読む」
という案 1 の素朴な形は、**削減量ゼロ**になる。削減は「読むエントリを減らす」ことでは
なく「**1 エントリあたりの読む量を減らす**」ことからしか出ない——つまり案 1 は案 2 に
吸収される（§3.1）。

### 2.4 着手順の選択基準は**モード依存**である（案 1・案 2 の設計制約）

`priority-policy.md` の各モードが「着手順の選択基準」で参照するものを実測した
（2026-08-27 時点・`active: normal`）:

| モード | 着手順が参照するもの | 索引で足りるか |
| --- | --- | --- |
| `normal`（現在 active） | 優先度（P0→P1→P2）→ 同一優先度内は**起票日が早い順** | **✓** |
| `gdd-first` | 明示された課題 ID の列挙と依存順 | **✓**（ID） |
| `improvement-first` | 明示された課題 ID の列挙と依存順、「同一リポジトリ・同一ファイル群に触れる組は 1 委譲に束ねてよい」 | **✓**（ID＋関連リポジトリ） |
| `gdd-dogfood-first` | 「**静かに壊れるものを先に着手する**」「失敗が観測されない欠陥」 | **✗ 内容判断が要る** |
| `release-freeze` | 「回帰修正・本番安定に関わるか」「新規機能追加系か」 | **△ タイトルで概ね判定可・確実性は説明依存** |
| `domain-bootstrap:<domain>` | `<domain>` に関連するか | **✓**（関連サービス・担当ポジション） |

**6 モード中 4 つは索引だけで着手順を決められる**が、`gdd-dogfood-first` は
エントリの**内容**を読まないと基準を適用できない。したがって設計上の要件は:

> 索引だけで着手順が決まらないモードでは、**分類済エントリを索引順に開いて読む**経路を
> 手順2 に持たせる。索引が無い今より悪くなることはない（今は全件を無条件に読んでいる）。

## 3. 各案の評価

削減量は「毎周ロードする台帳の tok」で比較する。基準は現行の **30,883 tok**
（ヘッダ＋記入例 1,204 tok を含む）。固定文脈の小計は §1.2 の 67,034 tok。

### 3.1 案 1 — ステータスによる絞り込み

- **(a) 削減見込み**: 素朴な形では **0 tok**（§2.3。全ステータスが生きている）。
  §2.2 に照らして「承認待ち系・完了は索引レベル、それ以外は全文」まで精密化すると
  **20,718 + 1,204 = 21,922 tok（−29.0%）**、固定文脈は 67,034 → 58,073（**−13.4%**）。
- **(b) 判断材料の充足**: 精密化した形なら欠けない（1-f/1-g/1-i は索引＋`git log` で足りる）。
  ただしこの精密化は「エントリごとに読む量を変える」ことであり、**実体は案 2 である**。
- **(c) フォーマット契約への波及**: **無し**（読み方の規律であって形式を変えない）。
- **(d) board への波及**: **無し**（board は `challenge-ledger.md` を自分で読む。
  `src/server/watcher.ts:17` の `LEDGER_FILE_NAME = "challenge-ledger.md"`）。
- **(e) `fp` への影響**: 無し。

**推奨: 不採用（案 2 へ吸収）。** 単独で採ると効果がゼロか、精密化した瞬間に案 2 になる。
「ステータスで対象を選ぶ」機能は案 2 の索引が担う。

### 3.2 案 2 — 索引 ＋ 必要分だけの読み出し

投影スクリプトの試作を書き、実台帳に対して実測した（本ドキュメントは提案のため
スクリプトは同梱していない。下記の擬似コードで再現できる）:

```ruby
# 台帳（正本）から索引を投影して stdout に出す。ファイルは作らない。
# エントリ境界は ^### \[ 、``` フェンス内は除外（validate-artifact.rb と同じ意味論）。
# 出力: id / status / prio / pos / opened / repos / approvals / title の TSV
```

実測結果（全 11 エントリ）:

| | バイト | tok | 1 エントリあたり |
| --- | ---: | ---: | ---: |
| 台帳の全文 | 100,652 | 30,883 | 2,698 |
| **投影した索引（TSV）** | **2,246** | **771** | **70** |

**索引は全文の 2.5%**（1 エントリ 70 tok ＝ 全文の 38 分の 1）。

毎周の台帳ロード量は「索引 771 tok ＋ その周に開いたエントリの全文」になる:

| その周に開くエントリ数 | 台帳ロード | 対現行 | 固定文脈 | 対現行 |
| ---: | ---: | ---: | ---: | ---: |
| 0 件（承認待ちだけの周） | 771 | −97.5% | 36,922 | −44.9% |
| 1 件 | 3,469 | −88.8% | 39,620 | −40.9% |
| 2 件 | 6,167 | −80.0% | 42,318 | −36.9% |
| 3 件 | 8,865 | −71.3% | 45,016 | −32.8% |
| 7 件（分類済＋着手中の**全件**＝最悪） | 20,016 | −35.2% | 56,167 | −16.2% |

> 「開くエントリ数」は §1.3 の平均 2,698 tok で概算した。最悪ケースは
> 分類済 7,070 ＋ 着手中 12,175 の実測値を使った。

**実運用での件数**: `journal/` の ① 触った課題 を数えると、直近 14 周で
**中央値 5 件 / 範囲 2〜13 件**（`awk '/^## 触った課題/,/^## 委譲/'` で ID を重複除去）。
ただしこのうち「ステータスだけを進めた件」（承認検出＝索引で足りる）が相当数を占め、
**全文が要るのは未分類の分類分と実際に着手した件**に限られる（§2.2）。
手順の規定にも「1 周で扱う件数に上限を設けてよい」（冪等性の原則）があるため、
運用上は 1〜3 件の帯に収まる設計にできる。

- **(b) 判断材料の充足**: §2.2 の対応表で、索引が持つべきフィールドは
  **ID・タイトル・ステータス・優先度・担当ポジション・起票日・関連リポジトリ・
  承認チェックの `[ ]`/`[x]` 状態**の 8 つと確定した。
  この 8 つで 1-c/1-f/1-g/1-i の判定・2-a（`gdd-dogfood-first` を除く 5 モード）・2-b/2-d/2-h が
  足りる。足りないもの（1-a/1-b/1-d/1-e/2-e/2-f/2-g、および `gdd-dogfood-first` の 2-a）は
  **索引順に該当エントリを開く**ことで充足する。
  **索引が課題の存在そのものを落とさない**（全エントリを 1 行ずつ投影する）ため、
  「絞り込みで課題を見落とす」経路は構造的に存在しない。
- **(c) フォーマット契約（`scripts/validate-artifact.rb`）への波及**: **無し**。
  台帳の形式を一切変えないため、`LEDGER_REQUIRED_LINES`・`LEDGER_REF_FIELDS`・
  結合切れ検査（6）・参照フィールドの形（7）はそのまま通る。
  **投影スクリプトを追加する場合は、その受理方向のテストが要る**（§6）。
- **(d) board への波及**: **無し**（台帳ファイルも形式も変わらない）。
  **ただし「索引ファイルを作って台帳を分割する」形にした場合は board が壊れる**——
  board は `LEDGER_FILE_NAME = "challenge-ledger.md"` という**固定の 1 ファイル**しか
  読まない（`src/server/watcher.ts:17`。2026-08-27 時点の `main`＝`091b6aa` で確認）。
  台帳から出したものは board から**見えなくなる**。
- **(e) `fp` への影響**: 無し（台帳本文が変わらない）。

**設計上の必須条件（`sync-free` 不変条件）**: 索引を**ファイルとして持ってはならない**。
「索引と本体を同期させる」構図は必ずずれる。索引は**台帳から毎回投影して stdout に出す**
（＝正本は台帳 1 つ、索引は射影であって第二のリストではない）。この形なら:

- `contracts/cycle-commit-paths.txt` に新しいパスを足す必要がない（生成物が無い）。
- `scripts/noop-check.rb` の判定に新しい dirty パスが現れない。
- 生成側と検証側が増えない＝ [generator-validator-shape-mismatch] の再発面が生じない
  （投影は台帳を読むだけで、台帳へは何も書かない）。

**推奨: 採用（ただし「投影スクリプト」の形に限る）。**
索引ファイルを repo に作る形での採用は**推奨しない**。

### 3.3 案 3 — 原文引用の置き場所（台帳から外し参照に寄せる）

- **(a) 削減見込み**: 引用行を落とすと台帳は **9,820 + 1,204 = 11,024 tok（−64.3%）**、
  固定文脈は 67,034 → 47,175（**−29.6%**）。
  案 2 の 0〜3 件シナリオ（−32.8%〜−44.9%）と**同等かやや劣る**。
- **(b) 判断材料の充足**: **欠ける**。1-a/1-b/1-d/1-e（分類・優先度判定）と
  2-e（タスク分解）が必要とするのは説明の**内容**である。参照だけになると、
  読むために毎回 `gh issue view` 等の外部取得が要る——それは案 2 の
  「必要な 1 件だけ開く」と同じ動作を、**外部ネットワーク依存・外部の可用性依存で**
  やることになる。案 2 の方が安全側である。
- **(c) フォーマット契約への波及**: 中程度。`LEDGER_REQUIRED_LINES` の `- 説明:` 行自体は
  残るので必須行検査は通る。ただし **`- 説明:` の値が常に `（原文引用）` という定型文字列に
  なる**ため、契約は「値があること」を検査していない現状では**空虚に真**になる
  （値の実質を検査していないので、壊れても契約が気付かない）。
- **(d) board への波及**: **視認できる退行が起きる**。
  `src/ui/components/CardDetailModal.tsx:310` は `{challenge.description ?? "-"}` で
  説明を表示する。パーサ（`src/server/parsers/ledger.ts`）は引用行を継続行として
  `description` に取り込む設計（`QUOTE_CONTINUATION_PATTERN = /^>/`）なので、引用を外すと
  **カード詳細の説明欄が `（原文引用）` の一語だけになる**。これは Issue #87 /
  claude-flywheel-board#151 で問題になった「値が落ちて `-` 表示になる」事故と同型である。
- **(e) `fp` 算出への影響**: **冪等性が壊れる。しかも既に一度潰した欠陥の再生産である。**
  `skills/ingest-challenges/SKILL.md` の `fp` 規定は次を明文で要求している:

  > **引用行を必ず含める**: 本スキルが書く説明欄は `- 説明:（原文引用）` ＋ **直下のブロック引用**
  > であり（実運用の主要形）、引用行を値から外すと説明の値が常に定型文字列 `（原文引用）` に
  > なる。すると**外部 Issue の本文だけが編集されたとき fp が動かず、永久にスキップされて
  > 人間記入欄が更新されない**（更新検知の不在を「変化なし」の証明に使わない）。

  さらに、`fp` 不一致時の自己修復条項——「**正規化済み本文同士を直接比較して実質同一なら**
  人間記入欄・取り込み日は更新せず `fp` のみ更新する」——は、**台帳側に旧本文が残っている
  ことを前提**にしている。引用を外すと比較相手が消えるため、この条項が機能しなくなる。
  `fp` を「取得した外部本文から算出する」形へ作り替えれば技術的には成立するが、
  **台帳だけからは `fp` を再計算・監査できなくなる**（現在は 8/8 の既存 `fp` を台帳から
  再現できることが実測済み＝[ingest-deterministic-fp]）。
- **(f) 恒久記録の喪失**: 手順4 の即アーカイブは「ステータス行以外は**原文のまま**追記」と
  規定されている。引用を外すと `challenge-archive.md` にも参照しか残らない。
  現在アーカイブは 57 エントリ・89,229 tok あり、そのうち **58,186 tok（65.2%）が引用**である。
  外部 Issue が編集・削除されれば、この 65% 相当の根拠が**回復不能に失われる**。
- **(g) `ingest-body-blockquote` の防御**: 引用を外せば「本文中の `##` が台帳のエントリ構造を
  壊す」経路自体が消えるので、その防御は不要になる。**ただし**参照だけを置く形でも
  タイトルを転記するなら、タイトル中の `#` に対する同種の防御は残す必要がある。

**推奨: 不採用。** 削減量は案 2 と同等以下でありながら、`fp` の更新検知・board の表示・
アーカイブの恒久記録という 3 つを同時に壊す。**案 2 は同じ削減量をこれらの破壊なしに得る。**

### 3.4 案 4 — 母数の削減（`assignee_policy: self-only`）

- **(a) 実測**: 2026-08-26 の 152,987 B（20 heads）→ 08-27 の 99,317 B（12 heads）。
  6 エントリの取り込み解除で **−53,670 B ≒ −16,400 tok**（−35%）。既に効いている。
- **(b) 判断材料の充足**: 欠けない（そもそも自分の担当でないエントリを持たない）。
- **(c)(d)(e)**: 波及なし（運用設定の変更であり形式・契約・`fp` に触れない）。
- **持続性の限界**: §1.4 のとおり、台帳は 08-25 → 08-26 で **1 日に 3.1 倍**に振れた。
  剪定は振れ幅の片側を戻すだけで、外部 ingest の流量そのものを制御しない。
  1 エントリ 2,698 tok なので、**6 エントリ増えれば案 4 の効果は消える**——
  これは 08-26 に実際に 13 エントリ増えた実績に照らして、数日で起きうる規模である。

**推奨: 継続（採用済み）。ただし案 2 の代替にはならない。**
案 4 は**母数**を、案 2 は**1 エントリあたりの単価**を下げる。両者は乗算的に効く:
案 4 適用後の 11 エントリ・30,883 tok に対して案 2 を重ねると、
0〜3 件シナリオで **771〜8,865 tok** になる。

## 4. 波及評価のまとめ

| | 案 1（素） | 案 1（精密化＝案 2） | 案 2（投影） | 案 3 | 案 4 |
| --- | --- | --- | --- | --- | --- |
| 台帳ロード | 30,883 | 21,922 | **771〜8,865** | 11,024 | 30,883（母数依存） |
| 固定文脈 | 67,034 | 58,073 | **36,922〜45,016** | 47,175 | — |
| 対現行 | 0% | −13.4% | **−32.8%〜−44.9%** | −29.6% | 実測 −35%（一過性） |
| `validate-artifact.rb` | 変更なし | 変更なし | 変更なし（＋投影の受理テスト） | 契約が空虚に真になる | 変更なし |
| board パーサ | 影響なし | 影響なし | 影響なし | **説明欄が退行**（#87 と同型） | 影響なし |
| ingest `fp` | 影響なし | 影響なし | 影響なし | **更新検知と自己修復が壊れる** | 影響なし |
| アーカイブ | 影響なし | 影響なし | 影響なし | **恒久記録の 65% を喪失** | 影響なし |
| `sync-free` | — | — | 満たす（投影・ファイル不作成） | — | — |

**board 側の確証**（`claude-flywheel-board` の `main`＝`091b6aa`・2026-08-27 実測）:

- `src/server/watcher.ts:17` — `export const LEDGER_FILE_NAME = "challenge-ledger.md"`。
  **1 repo につき固定名の 1 ファイル**しか読まない。台帳の分割は board を壊す。
- `src/server/parsers/ledger.ts:8-24` — `LedgerStatus` 型と `VALID_STATUSES` が
  7 値をハードコード。**本ドキュメントのどの案もステータス語彙を変えない**ため、
  この制約には抵触しない。
- `src/server/parsers/ledger.ts:389` — `needsHuman: status === "計画承認待ち" || status === "完了確認待ち"`。
  同上、影響なし。
- `src/server/parsers/ledger.ts:86-88` — `QUOTE_CONTINUATION_PATTERN = /^>/` により
  引用行を `説明` の値として取り込む。**案 3 はここに直撃する。**
- `src/ui/components/CardDetailModal.tsx:310` — `{challenge.description ?? "-"}`。

## 5. 効果の実測方法（採用後に確認すべきこと）

散文の見積もりで済ませないため、変更前後で次を実測する:

1. **台帳ロード量**: §1.1 のスクリプトを、変更前の台帳と「投影された索引 ＋ その周に
   開いたエントリの本文」に対してそれぞれ実行し、tok を比較する。
2. **固定文脈の小計**: §1.2 の表を再測定する（`CLAUDE.md` / `memory/*/INDEX.md` /
   `run-cycle/SKILL.md` / 台帳ロード量 / `challenge-sources.md`）。
3. **実周での確認**: 案 2 を入れた最初の 3 周について、journal の ① 触った課題 の件数と
   実際に開いたエントリ数を記録し、§3.2 の「開くエントリ数」の帯（1〜3 件）が
   現実に成り立つかを検証する。**成り立たない場合は削減率が −16.2%（最悪ケース）まで
   落ちる**ため、この検証は採否と同じ重みを持つ。
4. **報告に測った経路を必ず添える**（何を入力に・どのコマンドで・どの版で測ったか）。

## 6. 構造不変条件テストの設計（案 2 を採用した場合）

`run-cycle` は毎周走る中核であり、散文仕様は型検査が効かない。**変更前にテストで固定する。**
以下は設計案であり、実装は本ドキュメントの範囲外。

| # | テスト | 何を防ぐか |
| --- | --- | --- |
| T1 | **経路表の完全性**: 「ステータス × 手順 → 索引で足りる / 全文が要る」の真理値表を持ち、`LedgerStatus` の 7 値すべてに行があることを検査する。行の無いステータスは **fail**（fail-closed） | ステータスを足したとき、どの手順がそれを読むか未定義のまま出荷される |
| T2 | **投影の全域性**: 投影スクリプトの出力行数 ＝ 台帳のエントリ数（`^### \[`・フェンス除外）。1 件でもずれたら fail | 索引が課題を静かに落とす（[ledger-range-delete-bug] と同型の見落とし） |
| T3 | **受理方向**（[generator-validator-shape-mismatch] 対策）: `contracts/fixtures/ledger/valid/*` の全正例に対して投影が exit 0 かつ全エントリを出力すること | 「自分の正規出力を自分が受理しない」欠陥 |
| T4 | **単一エントリ抽出の境界**: `^### \[<ID>\]` から次の `^### \[` までの抽出が、隣接エントリの行を 1 行も含まないこと。**変異注入**（境界の直前・直後に行を足す）で検出できることまで確認する | 隣接エントリの巻き込み（[ledger-range-delete-bug]・再発 2 回） |
| T5 | **否定検査**: 投影の出力に引用行（`^>`）が 1 行も含まれないこと。含まれたら fail | 索引が肥大して「索引である」という前提が静かに失われる |
| T6 | **`sync-free` の固定**: 索引がファイルとして生成されないこと（`contracts/cycle-commit-paths.txt` に索引パスが現れない・`git status` に新規パスが出ない） | 索引ファイル化への漂流＝二重正本の発生 |
| T7 | **語彙駆動の結線**: `LedgerStatus` の 7 値を正本（例: `contracts/` の 1 ファイル）から読み、SKILL.md の手順見出しの「対象: ステータス『…』」と T1 の表が同じ語彙を使っていることを検査する | 3 箇所（board 型・SKILL.md・表）のずれ |

T1・T7 は既存の `scripts/tests/runtime-text-refs.test.sh`（散文検査・21 件）と同じ層に置ける。
T2〜T5 は投影スクリプトの単体テストとして `scripts/tests/` に追加する。

## 7. 採否の推奨（**決定ではない**）

| 案 | 推奨 | 根拠（実測値） |
| --- | --- | --- |
| **1. ステータスによる絞り込み** | **不採用**（案 2 へ吸収） | 素朴な形では削減 **0 tok**（7 ステータスすべてが手順の対象。§2.3）。精密化すると実体が案 2 になる |
| **2. 索引 ＋ 必要分だけの読み出し** | **採用（条件付き）** | 索引 **771 tok ＝ 全文の 2.5%**（1 エントリ 70 tok）。台帳ロード **−71.3%〜−97.5%**（0〜3 件シナリオ）、固定文脈 **−32.8%〜−44.9%**。契約・board・`fp` への波及**すべてゼロ**。**条件**: 索引はファイル化せず**台帳からの投影**（stdout）に限る（`sync-free`）。§6 の T1〜T7 を先に入れる |
| **3. 原文引用の置き場所** | **不採用** | 削減 −29.6% は案 2 と同等以下。一方で `fp` の更新検知が永久に効かなくなる（ingest-challenges が明文で禁じている形）・board の説明欄が退行（`CardDetailModal.tsx:310`）・アーカイブの恒久記録 **58,186 tok（65.2%）** を喪失。**案 2 が同じ削減を破壊なしに得る** |
| **4. 母数の削減** | **継続（採用済み）** | 実測 152,987 B → 99,317 B（**−35%**）。ただし 08-25 → 08-26 で **1 日 3.1 倍**に振れた実績があり、6 エントリ増で効果が相殺される。案 2 の代替にはならず、**乗算的に併用**するのが正しい |

## 8. 未決の論点

- **D-1**: 案 2 の索引は「投影スクリプト（Bash 実行）」に置くか、SKILL.md の
  散文手順（`awk` ワンライナー）として書くか。前者は文脈コストほぼゼロ
  （[run-cycle-context-budget.md §1.3] の実測: 正常系のスクリプト出力は 0 B）だが
  スクリプトが 1 本増える。後者は追加物ゼロだが SKILL.md が数百 tok 増える。
- **D-2**: `gdd-dogfood-first` のような**内容判断を要するモード**が active のとき、
  分類済エントリを何件まで開くか。上限を設けると着手順の質が落ち、設けないと
  最悪ケース（−16.2%）に張り付く。`priority-policy.md` のモード定義側に
  「索引で決まるか」を宣言させる案もあるが、モード追加時の記入漏れが
  fail-open になるため、既定を「開く」（安全側・削減を諦める）に倒す必要がある。
- **D-3**: 手順0 の台帳ロード規定を変えると、`--dry-run` 時の挙動と
  ロック・トランザクション回収（手順0 の先頭）との順序関係を再確認する必要がある。
  回収は台帳を**書く**前の検査であり読み込み方とは独立だが、規定の書き換えで
  順序が崩れないことをテストで固定したい（§6 T1 の対象）。
- **D-4**: 本ドキュメントの tok はすべて `o200k_base` による代理指標であり、
  **Claude 上での測定ではない**（[run-cycle-context-budget.md §1.1] と同じ限界）。
  比率・順序の結論はバイト数からも同じ向きに出る（台帳の B/tok は 3.26 で
  比較対象 7 ファイルの 3.26〜3.50 に収まる）が、絶対値は別トークナイザで変わりうる。
