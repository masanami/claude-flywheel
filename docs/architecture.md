# アーキテクチャ — claude-flywheel

> 要件（[requirements.md](./requirements.md)）と課題（[challenges.md](./challenges.md)）を **どう実現するか（How）** を定義する。
> 本書は実現方式・構成・主要フローを扱う。要件そのものの追加・変更は requirements.md 側で行う。

- ステータス: ドラフト
- 最終更新: 2026-06-17
- 関連: [requirements.md](./requirements.md) / [agent-memory.md](./agent-memory.md) / [challenges.md](./challenges.md)

---

## 1. 設計方針

1. **コントロールプレーンと実行プレーンの分離**
   - claude-flywheel = 「**何を・誰が**やるか」を司る制御層（課題台帳・ポジション・記憶・オーケストレーション）。
   - 実際のコード変更は対象サービス群（会社のマイクロサービス＝外部リポジトリ）に対して行う。
2. **ファイルベース・Git 管理**: 台帳・ポジション・記憶を Markdown ファイルで持ち、Git 履歴で変更を追跡（NFR-02 可観測性 / NFR-03 再現性）。
3. **Human-in-the-loop を構造に埋め込む**: 承認ポイントをフロー上の明示的なゲートとして配置（NFR-06）。
4. **Claude Code 基盤の活用**: エージェント＝Claude Code のエージェント／サブエージェント、定型手順＝スキルとして実装。
5. **実装は claude-harness へ委譲**: 具体的な実装・レビュー・E2E は既存の [claude-harness](https://github.com/masanami/claude-harness) のスキル／エージェントに委ねる。claude-flywheel は「課題の発見・分配・知識蓄積」に集中する。
6. **段階的自律化**: 初期は人間トリガ中心、信頼に応じてスケジュール実行・自動化へ移行（要件 §6）。

## 2. 全体構成

```
┌──────────────────────────── コントロールプレーン（claude-flywheel リポジトリ）────────────────────────────┐
│                                                                                                          │
│   人間 ──書く──▶  課題台帳 (challenge-ledger.md)                                                          │
│                        │ 参照                                                                            │
│                        ▼                                                                                 │
│                 オーケストレーター ──分類/横断判定/分配──┐                                                │
│                        │                                │                                                │
│         単一ドメインは自己選択                          │ 横断はサブ課題に分解して割当                    │
│                        ▼                                ▼                                                │
│        ┌───────────────┴───────────────┬───────────────┴───────────┐                                    │
│        ▼                               ▼                           ▼                                    │
│  ドメインAエージェント           ドメインBエージェント        ドメインCエージェント                       │
│   ├ ポジション (positions/A.md)                                                                          │
│   └ 記憶 (memory/A/: map/tacit/experience/reference)                                                     │
│        │ 実装が必要な具体タスクを委譲                                                                     │
└────────┼─────────────────────────────────────────────────────────────────────────────────────────────┘
         ▼
   ┌─────────────────────── 実行プレーン ───────────────────────┐
   │  claude-harness のスキル/エージェント                       │
   │  （implement-feature / write-e2e / para-impl / pr-merge …） │
   │        │ 変更対象                                           │
   │        ▼                                                    │
   │  会社のマイクロサービス群（外部リポジトリ）                  │
   └────────────────────────────────────────────────────────────┘
```

## 3. コンポーネント

### 3.1 課題台帳（Challenge Ledger）

- 実体: リポジトリ直下の単一ファイル [`challenge-ledger.md`](../challenge-ledger.md)。
- 人間が「人間記入欄」に課題を集約。オーケストレーターが「分類欄」を更新。
- 形式・分類ルール: [challenge-ledger-format.md](./templates/challenge-ledger-format.md)。
- 対応要件: FR-07。

### 3.2 オーケストレーター

- 役割: 台帳をトリアージし、各課題に **単一ドメイン / 横断 / 要相談** のタグを付与。横断課題はサブ課題へ分解し、主担当＋協働ドメインへ割り当てる。
- 実装: 台帳とドメイン地図（記憶 `map`）を入力に分類するエージェント（プロンプト＋ルールは `orchestrator/` に置く）。
- 「司令塔」ではなく「横断課題の交通整理役」。単一ドメイン課題は各エージェントの自己選択に委ねる。
- 対応要件: FR-08。判定に迷う場合は「要相談」として人間に上げる（承認ポイント）。

### 3.3 ドメイン担当エージェント

- 役割: 1 ドメイン（複数サービス横断）を担当。台帳から **自ドメインに関係する課題を自己選択**（FR-06）し、目標→タスク→実行→検証→学習を回す。
- 入力: 自身のポジション（`positions/<domain>.md`）と記憶（`memory/<domain>/`）。
- 実装が必要になったら、具体タスクを claude-harness のスキル/エージェントへ委譲する。
- 対応要件: FR-04〜06、FR-10〜13、FR-20〜23、FR-30〜32、FR-40〜42。

### 3.4 ポジション

- ドメイン担当エージェントの「職務記述書」。担当ドメイン／ミッション／関心範囲／権限／関係／関連度判定／記憶参照から成る。
- 定義: [templates/position.md](./templates/position.md)。ブートストラップでドラフト生成→人間承認。
- 配置: `positions/<domain>.md`。
- 対応要件: FR-04。権限欄が承認ポイントの線引き（OQ-02）を担う。

### 3.5 記憶ストア（Agent Memory）

- ドメインごとに `memory/<domain>/` を持ち、`map` / `tacit` / `experience` / `reference` を蓄積。`confidence` で推定/確証を区別。
- 運用: [agent-memory.md](./agent-memory.md)。
- 活用: タスク着手・レビュー前に関連記憶を前ロード（＝課題1ボトルネックBの解）。
- 対応要件: FR-40〜42、FR-09（`map` の生成元）。

### 3.6 ブートストラップ

- スキル [bootstrap-domain-map](../.claude/skills/bootstrap-domain-map/SKILL.md) が、対象サービス群を探索して **ドメイン地図 → ポジション案 → 記憶 seed** を生成。
- 出発点が「ドメイン未知」であるため、ポジション定義の前段として必須（FR-09）。
- 成果は人間が承認（承認ポイント）。

### 3.7 実行基盤と claude-harness 連携

- エージェント／サブエージェント、スキルは Claude Code 上で動作。
- 具体的な開発作業（実装・修正・E2E・レビュー・PR）は claude-harness のスキル群へ委譲する。
  - 例: 新規実装→`implement-feature`、変更→`modify-feature`、E2E→`write-e2e`、複数Issue並列→`para-impl`、マージ→`pr-merge`。
- claude-flywheel と claude-harness の責務分担:
  | | claude-flywheel | claude-harness |
  | --- | --- | --- |
  | 関心 | 何を・誰が（課題発見・分配・知識） | どう実装するか |
  | 単位 | ドメイン・ポジション | Issue・PR |

## 4. リポジトリ構成（コントロールプレーンの配置）

> OQ-01（管理基盤）に対する**提案**。当面は claude-flywheel リポジトリ内にファイルとして集約し、Git で追跡する。実行タスク（Issue/PR）は claude-harness 側で扱う。

```
claude-flywheel/
├── challenge-ledger.md          # 課題台帳（単一ソース）
├── positions/                   # ポジション定義（ドメインごと）
│   └── <domain>.md
├── memory/                      # エージェント記憶（ドメインごと）
│   └── <domain>/
│       ├── INDEX.md
│       └── {map,tacit,experience,reference}-*.md
├── orchestrator/                # オーケストレーターのルール/プロンプト
├── docs/                        # 設計ドキュメント
└── .claude/skills/              # スキル（bootstrap-domain-map 等）
```

- 変更対象の会社マイクロサービスは**外部リポジトリ**。エージェントはそれらを clone / worktree して作業する（claude-flywheel には含めない）。

## 5. 主要フロー

### 5.1 ブートストラップ（初期立ち上げ）

```
人間: 対象リポジトリ群を指定
  └▶ bootstrap-domain-map スキル
        1. インベントリ → 2. 関係抽出 → 3. 業務フロー推定 → 4. ドメインクラスタリング
        5. 生成: ドメイン地図 / positions/<domain>.md ドラフト / memory seed
  └▶ 人間レビュー（承認ポイント）: ドメイン境界・ポジション・暗黙知候補を承認/修正
  └▶ positions/ と memory/ が確定し、エージェントが稼働可能に
```

### 5.2 通常ループ（Flywheel）

```
1. 人間が課題台帳に課題を記述（人間記入欄）
2. オーケストレーターが分類（単一/横断/要相談）→ 分類欄を更新
3a. 単一ドメイン → 該当エージェントが自己選択して引き受け
3b. 横断 → サブ課題へ分解し各ドメインへ割当（主担当が統合）
4. エージェント: 関連記憶を前ロード → タスク探索・分解（人間承認: FR-13）
5. 実行: claude-harness へ委譲（破壊的/不可逆操作は人間承認: FR-22）
6. 検証: テスト・レビュー・動作確認（人間が最終確認: FR-32）
7. 学習: experience を記憶に追記、新たな暗黙知を tacit に追記（FR-40/41）
8. 報告: 状況・成果を人間へ（FR-51）
        └▶ 蓄積した記憶が次サイクルの前提知識になり、精度が上がる（弾み車）
```

### 5.3 横断課題（課題2の本丸）

```
オーケストレーター
  ├ サブ課題A → ドメインA（主担当）
  ├ サブ課題B → ドメインB（協働）
  └ 全体整合・順序を調整
        └▶ 各エージェントが自ドメイン分を実行 → 主担当が統合・検証
```

## 6. Human-in-the-loop 承認ポイント（実装位置）

| # | ポイント | 場所 | 要件 |
| --- | --- | --- | --- |
| 1 | ブートストラップ結果（ドメイン/ポジション/暗黙知）の承認 | 5.1 | FR-09 |
| 2 | 「要相談」課題の分類確認 | 3.2 / 5.2-2 | FR-08 |
| 3 | タスク起票の取捨選択 | 5.2-4 | FR-13 |
| 4 | 破壊的・不可逆操作（マージ/公開/削除/本番影響） | 5.2-5 | FR-22 |
| 5 | 検証の最終確認 | 5.2-6 | FR-32 |
| 6 | 暗黙知（confidence: low）の確証昇格 | 記憶保守 | FR-42 |

権限欄（ポジション §4）が、各エージェントの「自律可 / 要承認」の線引きを定義する。

## 7. 自律実行の仕組み

- **現段階（手動トリガ）**: 人間がオーケストレーター実行／各エージェント実行を起動する。
- **次段階（スケジュール実行）**: オーケストレーターを定期ジョブ化し、台帳の未分類課題を自動でトリアージ。各ドメインエージェントも定期的に自ドメインの課題を点検。
  - Claude Code のスケジュール実行（routines / cron）を実行基盤の候補とする。
- いずれの段階でも §6 の承認ポイントは維持し、自律度の引き上げは信頼に応じて段階的に行う（要件 §6、OQ-03）。

## 8. 段階的実装ロードマップ

| フェーズ | 目的 | 主な成果物 |
| --- | --- | --- |
| **P0 立ち上げ**（現在） | 概念・要件・骨子の確定 | docs 一式、テンプレ、課題台帳、bootstrap スキル |
| **P1 ブートストラップ実証** | 1 対象群でドメイン地図とポジションを生成 | `positions/`・`memory/` の初版、ドメイン地図 |
| **P2 単一ドメイン自走** | 1 ドメインで台帳→自己選択→実装委譲→検証→学習を一巡 | 1 ドメインの Flywheel 実証（受け入れ基準 §11） |
| **P3 オーケストレーション** | 複数ドメイン＋横断課題の分配 | オーケストレーター、横断フロー実証 |
| **P4 自律化** | スケジュール実行・自動トリアージ | 定期実行、自律度の段階引き上げ |

## 9. 要件トレーサビリティ

| 要件 | 実現コンポーネント |
| --- | --- |
| North Star（§1） | 課題台帳 + 自己選択（3.3）+ オーケストレーター（3.2） |
| FR-04〜06 | ポジション（3.4）+ ドメインエージェント（3.3） |
| FR-07 | 課題台帳（3.1） |
| FR-08 | オーケストレーター（3.2） |
| FR-09 | ブートストラップ（3.6）+ 記憶 `map`（3.5） |
| FR-20〜23（実行） | claude-harness 連携（3.7）+ 承認ポイント（6） |
| FR-30〜32（検証） | claude-harness 連携（3.7）+ 承認ポイント #5 |
| FR-40〜42（学習） | 記憶ストア（3.5）/ agent-memory.md |
| NFR-02/03 | ファイルベース・Git 管理（1.2） |
| NFR-06 | 承認ポイント（6） |

## 10. アーキテクチャ上の未決事項

- **AO-01**: 記憶／タスクの最終配置（OQ-01）。本書は claude-flywheel リポジトリ内集約を提案するが、実行タスクの Issue/PR 管理との接続を要確定。
- **AO-02**: オーケストレーターの実装形態（単発エージェント / スキル / ワークフロー / 定期ジョブ）。
- **AO-03**: ドメインエージェントの起動・常駐モデル（都度起動 / スケジュール / イベント駆動）。
- **AO-04**: 横断知識の共有方法（共有記憶領域 or ドメイン間相互リンク、agent-memory.md OQ）。
- **AO-05**: claude-flywheel と claude-harness のインターフェース（どの単位で何を渡すか）。
