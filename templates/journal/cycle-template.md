<!--
cycle-template.md — journal/YYYY-MM-DD-cycle.md の雛形。
run-cycle step 6 がコピーして日付・内容を埋める。セクション構成・順序は変えない（index.jsonl のフィールドと対応）。
同日 2 周目以降は `YYYY-MM-DD-cycle-2.md` のように連番サフィックスを付ける。
-->

# `<YYYY-MM-DD>` サイクルジャーナル（<n 周目 / 省略可>）

## 触った課題

- `<ID>`: `<旧ステータス> → <新ステータス>`

## 委譲

- repo: `<name>` / skill: `<実行スキル>` / session_id: `<claude -p の session_id>` / 結果: `<1行>`

## 作成した PR・ブランチの URL

- `<https://github.com/.../pull/NN>`

## 承認待ちゲート一覧

- `<FR-13 | FR-22 | FR-32>` — `<対象課題ID>` — `<1行要約>`

## 判断と根拠

- `<非自明な判断 1〜3行>`
